description: All data for a single script
allOf:
  - $ref: ./batchMetadataWithId.yaml
type: object
properties:
  jobs:
    type: array
    description: Summary information of all jobs in this batch
    items:
      $ref: ../jobs/jobMetadata.yaml
required:
  - jobs
